# Project Overview

Caddie is a local-only iPhone shopping-list application built with SwiftUI and Apple Foundation Models. It helps users maintain a persistent shopping list, organizes products by store aisle, remembers manual corrections, and keeps a manual path available when the local model cannot classify an item.

The product vocabulary is authoritative in `CONTEXT.md`. Product behavior and classification design are documented in `docs/design.md`.
